# nustar-sings

## Dependencies

Currently requires a local install of `nustar_pysolar` as well as `nustar-gen-utils` for
legacy reasons. Since I do not expect `nustar_pysolar` to stick around, we should move the
relevant IO module into this repo, instead.


